#ifndef COMPOSITOR_H
#define COMPOSITOR_H

namespace UI {
   // -------------------------------------------------------------------------
   // Compositor abstract base class
   //
   // This class is the base class for mechanisms to take UI widgets and
   // arrange them into flexible layouts.
   // -------------------------------------------------------------------------
   class Compositor {
   public:
      Compositor();

   protected:
   };
}
#endif
